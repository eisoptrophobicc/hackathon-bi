[
  {
    "intent_pattern": {
      "aggregation": "AVG",
      "metric": "{metric}",
      "filters": {},
      "group_by": "{dimension}"
    },
    "sql_template": "SELECT {dimension}, AVG({metric}) AS avg_{metric} FROM youtube_videos_staging GROUP BY {dimension}"
  }
]